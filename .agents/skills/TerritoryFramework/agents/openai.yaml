interface:
  display_name: "TerritoryFramework"
  short_description: "Repo-specific patterns and workflows for TerritoryFramework"
  default_prompt: "Use the TerritoryFramework repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true